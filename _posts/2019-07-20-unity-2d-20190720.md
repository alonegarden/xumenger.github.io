---
layout: post
title: Unity2D游戏开发
categories: 游戏开发之unity 
tags: Unity2D Unity 2D 3D 游戏开发 
---

## 角色移动

以下脚本实现让游戏角色根据玩家按下的W、S、A、D 键进行上下左右的移动

```C#
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/**
 * 控制角色移动、生命、动画……
 */
public class PlayerController : MonoBehaviour
{
    public float speed = 5f;    // 移动速度

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        // 根据WSAD输入控制角色上下左右移动
        float moveX = Input.GetAxisRaw("Horizontal");   // 控制水平移动方向，按A返回-1，按D返回1，否则0
        float moveY = Input.GetAxisRaw("Vertical");     // 控制垂直移动方向，按S返回-1，按W返回1，否则0

        // 获取角色当前位置
        Vector2 position = transform.position;

        // deltaTime是计算机渲染一帧需要的时间，这样可以控制每秒移动的速度
        position.x += moveX * speed * Time.deltaTime;
        position.y += moveY * speed * Time.deltaTime;

        // 为角色指定新的位置，完成角色的移动
        transform.position = position;
    }
}
```

## Tilemap绘制场景地图



## 
